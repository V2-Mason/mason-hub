# shared/ — 跨 Agent 共享资源

此目录存放所有 Agent 共享的工具脚本、配置模板和通用函数。

## 目录用途
- 通用 shell 函数（如通知、日志格式化）
- 跨 Agent 共享的配置片段
- 公共常量和环境变量模板

## 与 meta/ 的区别
- `meta/` — 知识文档（协议、知识库、路线图）
- `shared/` — 可执行代码和配置（被脚本 source 的函数库）
