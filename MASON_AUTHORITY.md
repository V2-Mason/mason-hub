# Mason 授权边界声明

> 最后更新: 2026-03-10
> 更新权: 仅 Mason 可修改此文件
> 所有 Agent 必须在 session 启动时读取此文件

## Mason 的角色

战略审批者。负责方向决策，不负责执行细节。

## 核心原则

1. **边界内全自主** — 授权范围内的事直接做完，不要一步步来问
2. **边界外批量问** — 收集所有待决策项，一次性问，不要每遇到一个小问题就打断
3. **先自转再扩张** — 当前圈层稳定运转后，才解锁下一层能力

---

## 圈层模型：从内到外，逐层稳定

```
┌─────────────────────────────────────────────┐
│  Layer 4: 归因分析（未来）                     │
│  ┌─────────────────────────────────────┐     │
│  │  Layer 3: 数据分析（下一步）           │     │
│  │  ┌─────────────────────────────┐     │     │
│  │  │  Layer 2: 自主执行 ← 当前     │     │     │
│  │  │  ┌─────────────────────┐     │     │     │
│  │  │  │  Layer 1: 基础运转   │     │     │     │
│  │  │  │  （已稳定 ✅）       │     │     │     │
│  │  │  └─────────────────────┘     │     │     │
│  │  └─────────────────────────────┘     │     │
│  └─────────────────────────────────────┘     │
└─────────────────────────────────────────────┘
```

---

### Layer 1: 基础运转 ✅ 已稳定

**能力**：agent 能跑、cron 能触发、代码能部署、数据能采集

**自主权限**（全部直接执行）：
- 代码格式标准化、重构、bug 修复
- 脚本语法检查与修复
- 新文件/目录创建（符合现有结构）
- .gitignore / 日志 / 缓存处理
- 文档更新（反映已完成的工作）
- Agent 记忆更新（遵循铁律 3）
- Backlog 更新（标记完成、添加发现的问题）
- 标准库范围内的依赖引用

**稳定标准**：cron 连续 7 天正常运行，无手动干预 — ✅ 已达成

---

### Layer 2: 自主执行 ← 当前正在建立

**能力**：Agent 在授权范围内自主决策，Mason 从操作者变成审批者

**自主权限**（Layer 1 全部继承 + 以下新增）：
- cron 任务注册（触发时机已明确的）
- 已有规范的落地实施（按既定架构执行）
- 数据格式转换（方向已定的）
- 数据目录注册新数据集 / Schema 定义
- 散落文件 ownership 分配
- 配置文件修改（不涉及费用或外部服务）
- 测试编写与执行

**执行后通知即可**：
- 新增依赖包（非标准库）
- Agent 配置文件修改（职责/启动流程/协作关系）
- CLAUDE.md 事实性更新（cron 表、agent 列表）
- 跨 agent 协作接口变更

**稳定标准**（全部满足才算稳定）：
1. 连续 3 天 Mason 不主动介入，系统正常运转
2. data_health_check.sh 连续 3 天全绿（无 ❌）
3. optimization-cycle 读到的是干净数据（从 mirror/JSONL，不是 raw markdown）
4. morning briefing 数据来源可追溯（每条信息标注来源数据集）

**解锁条件**：Mason 确认 "Layer 2 稳定了，开始 Layer 3"

**注意**：Layer 3 的"分析能力"需要等 Layer 2 稳定，但 Layer 3 的"数据基础设施"（SDK、管道清洁度）是 Layer 2 稳定的前置条件，属于 Layer 2 的工作范围。

---

### Layer 3: 数据分析（下一步，Layer 2 稳定后解锁）

**能力**：数据读取 SDK 就绪，业务 agent 能基于干净数据做分析

**预计新增自主权限**：
- 数据读取 SDK 接口设计与实现
- 加工层标准化（raw→clean→analysis→report）
- optimization-cycle 读 JSONL 替代 raw markdown
- 分析脚本的指标口径定义（按 data_catalog 标准）

**预计仍需确认**：
- 新指标的业务含义（如"互动分"权重调整）
- 分析结论的对外输出（发 Slack 给 Mason 的内容格式）

**稳定标准**：能回答 "上周哪个内容效果最好，为什么"，数据源头到结论全链路自动化

---

### Layer 4: 归因分析（未来，Layer 3 稳定后解锁）

**能力**：因果推断，区分相关性和因果性，定量评估活动效果

**预计新增自主权限**：
- A/B 测试设计与执行
- 归因模型选择（时间窗关联 / 增量分析）
- 自动生成归因报告

**预计仍需确认**：
- 归因结论影响业务决策时（如 "该关键词应淘汰"）
- 预算分配建议

**稳定标准**：做了一次活动，能定量评估增量效果

---

## 所有圈层共享的硬性红线（永远需要 Mason 确认）

### 费用与外部
- 引入新的外部服务或产生费用
- 涉及对外接口（平台 API、合作方、第三方服务）
- API key 配置或变更

### 架构与方向
- 架构方向有多个方案需要权衡
- 第一次做某类事情，没有先例可循
- 新 Agent 创建（角色定义需要 Mason 确认）

### 数据与安全
- 数据删除或不可逆操作
- 覆盖已有文件（先备份，告知差异）
- 涉及密钥、凭证、敏感信息
- 生产环境部署（阿里云）

### 业务决策
- 品牌定位、定价、供应商相关
- XHS 账号操作（任何涉及封号风险的）
- 推广预算、付费服务

---

## 开会模式（执行前对齐协议）

### 何时触发

包含 3 个以上步骤的任务批次，开始前必须输出**执行计划确认单**。

### 确认单格式

```
执行计划确认单
==============

任务批次：<批次名称>
当前圈层：Layer N

【我将自主执行（Layer N 授权范围内）】
1. <任务描述> → <为什么不需要确认>
2. ...

【需要你确认的，请一次性回答】
Q1：<决策项>
  背景：<为什么需要决策>
  我的推荐：<推荐方案和理由>
  你的选择：[接受推荐] / [其他方案：___]

Q2：...

---
请回复上面 N 个问题，我收到后立即开始全部执行，中途不再打断你。
```

### 执行中规则

- 当前 Layer 授权范围内 → 自己决定，commit message 记录
- 超出当前 Layer → **不要立刻问**，收集起来，凑齐后一次性问
- 绝不每遇到一个小问题就打断 Mason

---

## 圈层扩张机制

### 升级到下一层
- 当前层稳定标准达成 → Agent 汇报 Mason
- Mason 确认 "Layer N 稳定了" → 解锁 Layer N+1
- 更新本文件，将 Layer N+1 的"预计"权限变为正式权限

### 层内扩张（自动）
- Mason 在同类决策上连续 3 次选择了推荐方案 → 该类决策升级为自主权限
- 记录在下方"已扩张边界"

### 手动升级
- Mason 明确说 "以后这类事你自己决定" → 立即更新

### 降级
- Mason 说 "这个你不该自己决定" → 立即移回"必须确认"
- 记录原因，防止重复

---

## 已扩张边界（随使用积累）

> 格式：[日期] Layer / 决策类型 — 原因

- [2026-03-10] L2 / 轻量 vs 重量方案选择 — Mason 倾向务实，演进路径 A→C→B 已定
- [2026-03-10] L2 / 文件 ownership 分配 — 所有文件必须有 owner，直接分配不需要问
- [2026-03-10] L2 / 数据集注册 — 发现新数据源直接注册到 catalog
